








export const SchoolService = {
    async addSchool(data: {
        name: string,
        address: string,
        latitude: number,
        longitude: number
    }) {
        // await 
    }
}